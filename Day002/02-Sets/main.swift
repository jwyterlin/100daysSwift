// Creating a set from an array
let colors = Set(["red", "green", "blue"])

// Duplicated colors don't appear repeated in the set
let otherColors = Set(["white", "yellow", "brown", "yellow", "cyan"])
